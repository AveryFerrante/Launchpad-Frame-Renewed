import { ImageTransitionDirective } from './image-transition.directive';

describe('ImageTransitionDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageTransitionDirective();
    expect(directive).toBeTruthy();
  });
});
